{
  "buildCommand": "cd app && npm install && npm run build",
  "outputDirectory": "app/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://daily-ledger-production.up.railway.app/api/$1" },
    { "source": "/((?!.*\\.).*)", "destination": "/index.html" }
  ]
}
